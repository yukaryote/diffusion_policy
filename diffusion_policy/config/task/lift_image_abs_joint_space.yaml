name: lift_image_abs_joint_space

shape_meta: &shape_meta
  # acceptable types: rgb, low_dim
  obs:
    agentview_image:
      shape: [3, 84, 84]
      type: rgb
    robot0_eye_in_hand_image:
      shape: [3, 84, 84]
      type: rgb
    robot0_gripper_qpos:
      shape: [2]
      # type default: low_dim
    # robot0_gripper_qvel:
    #   shape: [2]
    robot0_joint_pos:
      shape: [7]
    # robot0_joint_pos_cos:
    #   shape: [7]
    # robot0_joint_pos_sin:
    #   shape: [7]
    # robot0_joint_vel:
    #   shape: [7]
  action: 
    shape: [9]

task_name: &task_name lift
dataset_type: &dataset_type ph
dataset_path: &dataset_path data/robomimic/datasets/${task.task_name}/${task.dataset_type}/image_abs.hdf5
abs_action: &abs_action True

env_runner:
  _target_: diffusion_policy.env_runner.robomimic_image_runner_joint_space.RobomimicImageRunnerJointSpace
  dataset_path: *dataset_path
  shape_meta: *shape_meta
  env_target: LiftJointSpace
  n_train: 10
  n_train_vis: 2
  train_start_idx: 0
  n_test: 22
  n_test_vis: 4
  test_start_seed: 100000
  # use python's eval function as resolver, single-quoted string as argument
  max_steps: ${eval:'500 if "${task.dataset_type}" == "mh" else 400'}
  n_obs_steps: ${n_obs_steps}
  n_action_steps: ${n_action_steps}
  render_obs_key: 'agentview_image'
  fps: 10
  crf: 22
  past_action: ${past_action_visible}
  abs_action: *abs_action
  tqdm_interval_sec: 1.0
  n_envs: 28
# evaluation at this config requires a 16 core 64GB instance.

dataset:
  _target_: diffusion_policy.dataset.robomimic_replay_image_joint_space_dataset.RobomimicReplayImageJointSpaceDataset
  shape_meta: *shape_meta
  dataset_path: *dataset_path
  horizon: ${horizon}
  pad_before: ${eval:'${n_obs_steps}-1+${n_latency_steps}'}
  pad_after: ${eval:'${n_action_steps}-1'}
  n_obs_steps: ${dataset_obs_steps}
  abs_action: *abs_action
  rotation_rep: 'rotation_6d'
  use_legacy_normalizer: False
  use_cache: False
  seed: 42
  val_ratio: 0.02
